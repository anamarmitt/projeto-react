import React from "react";
import Header from "../../../components/Header/Header";

const ProductsScreen = (props) => {
  const { goToCartScreen } = props;
  return (
    <>
      <h1>Products Screen</h1>
    </>
  );
};

export default ProductsScreen;
